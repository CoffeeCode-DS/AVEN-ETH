# AVEN-ETH frontend configuration
# No environment variables are required to run the app locally — the
# Vite dev server proxies all /api requests straight to the backend
# (see client/vite.config.js).
#
# If you change the backend's PORT away from 4000, update the proxy
# target in vite.config.js to match.
